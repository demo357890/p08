# p08




mkdir p8
cd p8
touch file.txt
git add .
git commit -m "hello"
git checkout -b s-brach
touch file.txt
git add .
git commit -m "hello"
touch file3.txt
git add .
git commit -m "hello"
git log
git checkout master
git cherry-pick code
